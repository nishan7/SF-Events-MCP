{
  "name": "sf-events-ui",
  "version": "1.0.0",
  "description": "Custom UI for SF Recreation & Parks Events",
  "scripts": {
    "build": "esbuild src/component.tsx --bundle --format=esm --outfile=dist/component.js --jsx=automatic",
    "watch": "esbuild src/component.tsx --bundle --format=esm --outfile=dist/component.js --jsx=automatic --watch"
  },
  "dependencies": {
    "mapbox-gl": "^3.15.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "@types/react-dom": "^18.2.0",
    "esbuild": "^0.19.0",
    "typescript": "^5.0.0"
  }
}
